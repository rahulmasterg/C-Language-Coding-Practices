#include <stdio.h>

int main()
{
    int arr[5];
    int len = sizeof(arr)/sizeof(int);
    
    for(int i=0; i<len; i++){
        int a;
        scanf("%d", &a);
        arr[i] = a;
    }
    
    for(int i=len-1; i>=0; --i){
        printf("%d ", arr[i]);
    }

    return 0;
}

